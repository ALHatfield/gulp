
5)
    gulp package
      - zip up build files?
      - log kb size
      https://www.npmjs.com/package/archiver

6)
    footnotes
      - use tables to force bullets in correct position?


7)
    `_banner_` needs to be deleted everytime to run generate.js





13)   gulpfile.js Package file function:

      .pipe(gulp_remove_logging())   - not working


14)
    not getting build:
    $frame4 = doc.querySelectorAll(".frame4");
    $frame4_in = doc.querySelectorAll(".frame4-in");
    $frame4_out = doc.querySelectorAll(".frame4-out");

15)
    if no ISI then set override.scss isi width to 0

16)
    create guide buttons for developer mode